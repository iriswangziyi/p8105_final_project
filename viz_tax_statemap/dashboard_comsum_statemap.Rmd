---
title: "Beer Consumption (Per Capita in Barrels)"
output: 
  flexdashboard::flex_dashboard
---

```{r setup, include=FALSE}
library(tidyverse)
# devtools::install_version("plotly", version = "4.8.0", repos = "cran.us.r-project.org")  #install lower version for proper layout
library(plotly)
library(flexdashboard)
```

```{r}
state_df = read_csv("../data_import_cleaning/state_df.csv")
# View(beer)
# View(state_df)

consum_df = state_df %>%
  select(state, consumption, year) %>%
  pivot_wider(
    names_from = "year",
    values_from = "consumption"
  )
# View(consum_df)

state_info = #arrange row order
  tibble(
    state = state.name, # arrange row order according default state.abb
    state.abb = state.abb
  )

consumption <- 
  left_join(state_info, consum_df, by = "state")

g <- list(
  scope = 'usa',
  projection = list(type = 'albers usa'),
  lakecolor = toRGB('white')
)
```

Map
=====================================  
This data is gotten from [America's Beer Distributors](https://www.nbwa.org/resources/state-data). 


Column
-------------------------------------
    
### Overall Trend
    
```{r warning=FALSE, message=FALSE}
state_df %>%
  filter(year > 2009) %>%
  mutate(text_label = str_c("state: ", state)) %>% # creating text label 
  plot_ly( 
    x = ~year, y = ~consumption, color = ~factor(state), text = ~text_label,
    alpha = .5, type = "scatter", mode = "lines", colors = "viridis") %>%
  layout( # adding a title 
    title = "Per Capita Consumption over the Years")
```

Column {.tabset .tabset-fade}
-------------------------------------
### Year 2010
```{r}
consumption_2010 <- consumption %>% pull(10)
plot_geo() %>%
  add_trace(
    z = ~consumption_2010 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2011
```{r}
consumption_2011 <- consumption %>% pull(11)
plot_geo() %>%
  add_trace(
    z = ~consumption_2011 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2012
```{r}
consumption_2012 <- consumption %>% pull(12)
plot_geo() %>%
  add_trace(
    z = ~consumption_2012 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2013
```{r}
consumption_2013 <- consumption %>% pull(13)
plot_geo() %>%
  add_trace(
    z = ~consumption_2013 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2014
```{r}
consumption_2014 <- consumption %>% pull(14)
plot_geo() %>%
  add_trace(
    z = ~consumption_2014 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2015
```{r}
consumption_2015 <- consumption %>% pull(15)
plot_geo() %>%
  add_trace(
    z = ~consumption_2015 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2016
```{r}
consumption_2016 <- consumption %>% pull(16)
plot_geo() %>%
  add_trace(
    z = ~consumption_2016 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2017
```{r}
consumption_2017 <- consumption %>% pull(17)
plot_geo() %>%
  add_trace(
    z = ~consumption_2017 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2018
```{r}
consumption_2018 <- consumption %>% pull(18)
plot_geo() %>%
  add_trace(
    z = ~consumption_2018 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

### Year 2019
```{r}
consumption_2019 <- consumption %>% pull(19)
plot_geo() %>%
  add_trace(
    z = ~consumption_2019 , text = state.name, span = I(0),
    locations = state.abb, locationmode = 'USA-states'
  ) %>%
  layout(geo = g)
```

Data
=====================================  
This data is gotten from [America's Beer Distributors](https://www.nbwa.org/resources/state-data). 

```{r}
knitr::kable(consumption[,-(3:9)])
# rmarkdown::render("dashboard_comsum_statemap.Rmd", output_format = "flexdashboard::flex_dashboard") #knit for web
```